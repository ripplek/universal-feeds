{
  "name": "universal-feeds",
  "version": "0.1.0",
  "private": false,
  "description": "Universal feed aggregation framework for AI agents (MCP server + CLI; Clawdbot/OpenClaw/Claude Code)",
  "license": "MIT",
  "bin": {
    "universal-feeds": "bin/digest",
    "universal-feeds-mcp": "bin/mcp"
  },
  "type": "module",
  "scripts": {
    "test:unit": "node --test",
    "test:smoke": "node bin/digest --config config/feeds.ci.yaml --date today",
    "test": "npm run test:unit && npm run test:smoke",
    "prepare": "husky"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "yaml": "^2.8.1"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "prettier": "^3.9.4"
  }
}
