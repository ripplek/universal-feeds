{
  "name": "universal-feeds",
  "version": "0.1.0",
  "private": false,
  "description": "Universal feed aggregation framework for Clawdbot/OpenClaw",
  "license": "MIT",
  "bin": {
    "universal-feeds": "bin/digest"
  },
  "type": "module",
  "scripts": {
    "test:unit": "node --test",
    "test:smoke": "node bin/digest --config config/feeds.ci.yaml --date today",
    "test": "npm run test:unit && npm run test:smoke",
    "prepare": "husky"
  },
  "dependencies": {
    "yaml": "^2.8.1"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "prettier": "^3.9.4"
  }
}
